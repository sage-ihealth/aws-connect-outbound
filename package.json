{
  "name": "aws-connect-outbound",
  "version": "1.0.0",
  "description": "sls invoke --function hello",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sage",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1484.0",
    "mongoose": "^7.6.4"
  }
}
